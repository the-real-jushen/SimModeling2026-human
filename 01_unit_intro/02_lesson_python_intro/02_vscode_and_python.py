# %% [markdown]
"""
# 锋其刃：配置 Python 开发环境与 VS Code

在了解了“为什么这门课要学习 Python”之后，接下来的问题非常务实：“我们到底要在哪里写代码？”

写代码需要一个专门的工具，这就好比你写论文需要用 Microsoft Word，画图纸需要用 AutoCAD。在专业的编程领域，我们把用来写代码的软件称为 **IDE，即集成开发环境 (Integrated Development Environment)**。

对于 Python 而言，市面上有许多优秀的 IDE。比如：
- 专门做科研、数据可视化的 **Jupyter Notebook**。
- 适合开发大型后端项目的原生 **PyCharm**。
- 被许多高校实验室采用的 **Spyder**。

但在本课程中，我们强烈推荐并且将全程采用的工具是：**Visual Studio Code (简称 VS Code)**。

## 为什么是 VS Code？

VS Code 是由微软开发的一款免费、开源、轻量级但功能极其强大的代码编辑器。
在当今的程序员世界中，它几乎统治了半壁江山。它的绝招在于“扩展性”——通过安装各种插件 (Extensions)，它几乎可以变成任何语言的顶级开发环境。

对于我们这些用 Python 进行“数学建模”和“工程数据分析”的用户来说，VS Code 拥有一个绝对杀手级的特性：**它完美地融合了传统 Python 脚本（.py）的高效性与 Jupyter Notebook 的交互性。**

![VS Code Python Interactive Window](https://code.visualstudio.com/assets/docs/python/jupyter/code-cells-02.png)

"""

# %% [markdown]
"""
## 什么是 Jupyter Notebook 的“交互式”模式？

为了理解为什么我们需要这种模式，想象一下传统程序员的工作流：
假设你写了一个包含 300 行代码的普通脚本，用来分析今年的全国气象数据。
1. 第一步：你需要从网上下载 10GB 的原始数据文件，它需要花 5 分钟加载进内存。
2. 第二步：你对数据进行清洗和建模运算。
3. 第三步：你在最后一行代码画出了一张温度预测地图，然后点击运行。

**痛点来了**：5分钟后程序运行结束了，但你发现画出来的地图上，图例被挡住了。你只需要改动一小行代码。修改完后，按照传统的脚本执行方式，你只能**再次点击运行，然后被迫再等待 5 分钟去重新加载那 10GB 的数据**。这对于每天要尝试百种算法进行数据探索的系统工程师来说，无疑是灾难性的。

**Jupyter 交互式编程** 完美解决了这个问题。
它引入了 **“代码单元格 (Cell)”** 的概念。
你可以把整篇代码分成一小块一小块的执行。如果你在第一个单元格加载了数据，跑完后数据就会**一直保留在内存里**！然后你可以在第二个单元格里随意、反复地修改画图代码并执行，每次只需几毫秒。

这种执行方式被称为 **REPL (Read-Eval-Print Loop)**，是数据科学与数学建模最核心的工作模式。

"""

# %% [markdown]
"""
## 在 VS Code 中使用 `# %%` 魔法 (Jupytext)

传统的 Jupyter Notebook 使用的是 `.ipynb` 格式的文件。但这种文件在实际工程团队中有一个巨大的致命缺陷：**它非常不利于版本控制工具（如 Git）的合并**，因为它本质上是一个乱糟糟的巨大 JSON 结构文件，一旦代码产生冲突，几乎无法人工解决。

为了兼顾“普通 Python 文件的清爽”和“Jupyter 的交互性”，VS Code 提供了一个极其完美优雅的解决方案：**在普通的 `.py` 后缀脚本中，使用 `# %%` 标记。**

只要你在标准的 Python 文件中输入 `# %%`（这就是我们在本课程模板中大量使用的写法），VS Code 的 Python 扩展就会立刻施展魔法，把下面的一段代码识别为一个**独立可以运行的单元格 (Cell)**。

### 它的核心特性：
1. **独立运行与局部调试**：你在每个 Cell 上方会看到一个小小的 `Run Cell` 按钮。点击它（或使用快捷键 `Shift + Enter`），你可以只执行这一段代码。
2. **内核状态保留**：当你第一次运行某个 Cell 时，VS Code 会在后台启动一个隐藏的 Python 引擎 (Kernel)。在这个引擎中，上面单元格定义的变量，下面单元格可以直接白嫖使用。
3. **沉浸式图文排版**：如果你的第一行是 `# %% [markdown]`，VS Code 就知道这个单元格纯粹是用来写笔记的，它会被即时渲染成漂亮的图文笔记。**这就是你这门课讲义的底层实现原理！**

"""

# %% [markdown]
"""
## 纸上得来终觉浅，让我们实操一下！

光听理论是学不会编程的。既然你现在正看着这份讲义，说明你的 VS Code 环境已经准备就绪。

在下方的代码单元格中，我们准备了两段简单的 Python 代码。我们将先定义两个工程变量（课程名称、学生人数），然后在第二个单元格中把它们打印出来。

**请你亲自用鼠标点击下面每个代码块上方出现的 `Run Cell` 跑一下试试（或者选中代码块后按下 `Shift + Enter`）。** 你会看到右侧或下方的“交互式窗口”瞬间弹出了执行结果！

"""

# %%
# 这是一个真实的代码单元格 (Code Cell)
# 请点击上方小字的 "Run Cell" 运行它，你会看到左侧或下方弹出交互式窗口！

course_name = "Intro to Mathematical Modelling"
robot_count = 100

print(f"✅ 成功在内存中定义了变量！课程名称是：{course_name}")




# %%
# 这里是第二个代码单元格。
# 这里不需要重新定义变量，我们能直接继承并调用上一个 Cell 存在内存里的信息！

print(f"👉 欢迎来到 {course_name}！")
print(f"👉 如果每个学生配备2台机器，我们需要控制 {robot_count * 2} 台机器人终端。")

# 动手挑战：请你尝试修改这行代码，把 robot_count 乘以 10，然后再次点击这个单元格的 "Run Cell" 看结果！



