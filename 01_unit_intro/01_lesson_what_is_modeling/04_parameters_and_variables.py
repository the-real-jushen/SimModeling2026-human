# %% [markdown]
"""
# 工程师的必修课：深入理解参数与变量

在数学和工程的交汇处，有两个概念如同空气和水一样不可或缺，它们就是**参数 (Parameters)** 和 **变量 (Variables)**。

很多人在高中学数学时，觉得所有的英文字母（如 $a, b, x, y$）都是一回事，无非都是代数符号。但在建立工程模型，特别是当你需要用 Python 去编写长篇大论的仿真代码时，如果将两者混淆，你的模型逻辑将彻底崩溃。

从最宏观的视角来看，任何一个系统模型都可以被抽象为一个广义函数：

$$ 	ext{输出状态} = f(	ext{输入状态}, 	ext{系统固有属性}) $$

用更数学的符号表示就是：
$$ y = f(x, 	heta) $$

在这个极简模型中，$f$ 代表物理规律（比如热力学定律）或转化算法，而里面包含的各式字母，分别扮演着截然不同的角色。

## 1. 什么是参数 (Parameters, 通常表示为 $\theta$)

在工程语境下，参数代表的是：这个被建模对象（或系统）**在当前这个特定版本下，固有且不可改变的物理特征或设计规格。**

**官方定义视角：**
> "Parameters are usually used for defining some characteristics of the modeled object."
> （参数通常用于定义被建模对象的某些特征，以确立其在特定语境下的唯一性。）

这句话什么意思？
想象一下你正在用建模软件（或者我们在玩赛车游戏时）生成一辆汽车。
决定这辆汽车是一辆**重型卡车**还是一辆**轻量化跑车**的数据，就是参数。
- 汽车的**空车重量**（比如 $M = 2000	ext{kg}$）
- 发动机的**最大马力**（比如 $P_{max} = 1000	ext{hp}$）
- 轮胎的**抓地力系数**（比如 $\mu = 0.8$）
- 甚至电池的**最大容量**

这些都是参数（Parameters）。在一次普通的百公里加速摸拟实验中，只要这辆车（我们的特定模型）被制造出来了，这些数值在**整个测试过程中是绝对不会发生改变的**。它们像“出厂设置”一样锁死了在这辆车上。如果你把 $2000	ext{kg}$ 改成了 $1000	ext{kg}$，不好意思，在数学模型的层面，你已经摧毁了上一辆车，凭空“造”出了一辆全新的车。

"""

# %% [markdown]
"""
## 2. 什么是变量 (Variables, 通常表示为 $x, y$)

与仿佛“焊死”的参数不同，变量是用来描述系统**瞬息万变的动态存在**的。

**官方定义视角：**
> "Variables are usually describing the state of the model."
> （变量通常用以描述模型在某一瞬间的状态与所处的环境。）

如果参数是汽车的“出厂配置”，那么变量就是你在**驾驶过程中的实时仪表盘数据**。
在模型启动、开始随时间推移进行计算的过程中，变量的值是**一直可以并且必须发生改变的**。

继续我们的赛车模型，在一次百公里直线加速测试中：
- 你踩下**油门的深度**（比如从 0% 踩到 100%，这是你施加外界影响的**输入变量 $x$**）
- 汽车此刻所处的**时间**（$t$ 从 $0	ext{s}$ 开始流逝，这是系统的**自变量 / 环境变量**）
- 汽车仪表盘上显示的**瞬时速度**（比如从 $0$ 飙升到 $100	ext{km/h}$，这是由系统内部运转产生的**输出变量 $y$**）
- 发动机当前的**转速RPM**（这是在系统内部隐藏变化的**状态变量**）

看出来区别了吗？
在一场测试（或者说一次仿真代码的运行期间）中：
**参数 ($	heta$) 稳如泰山，它定义了你是谁；**
**变量 ($x, y, t$) 随风舞动，它记录了你在干什么。**

"""

# %% [markdown]
"""
## 变量的分类：谁主导了谁？(Independent vs. Dependent)

当我们锁定了一组特定的变量去观察系统的动态时，我们往往需要理清它们之间的“因果关系”，因为这决定了我们写 Python 代码时的求解顺序。在数学上，我们将变量进一步拆分为两大阵营：

1. **自变量 (Independent Variable)**：它就是“因果”中的“因”。在当前的研究中，它是外界赋予系统的条件，是可以**自由变化、主动主导**的变量。（通常对应上面的 $x$ 或时间 $t$）。
2. **因变量 (Dependent Variable)**：它是“因果”中的“果”。在系统中，它是被迫防守的一方，它的值不能凭空给定，只能**被动地依赖于**自变量和方程的计算结果。（对应上面的 $y$）。

**注意：这种因果身份并不是绝对的！它完全取决于你要解决的工程目标是什么。**

让我们用最生活化的“踩油门”这个动作来理解这种身份翻转：

- **场景 A（正向仿真）**：我想知道我死死踩住 **$50\%$ 的油门**，持续 **$5秒钟**后，车子的**速度**能达到多少？
  - 这里，你主动给出的是油门和时间，所以【油门深度】和【时间】是**自变量**。
  - 你等待系统告诉你结果是什么，所以求出来的【速度】是**因变量**。

- **场景 B（反向设计/控制）**：我想让车子**严格按照 $120	ext{km/h}$** 的速度在高速上巡航，此时面临**每秒都在变化的逆风风速**，请问我的车载电脑应该施加多大的**油门**才能稳住？
  - 这里，你的目标是被强制定死的，外部环境也是给定的，所以【目标速度】和【风速】变成了给入的**自变量**。
  - 你需要计算并调整油门大小去迎合目标，所以求出来的【油门深度】反而变成了被迫防守的**因变量**。

这就是为什么理解分类如此重要。明确谁是自变量、谁是因变量，决定了你的代码循环该怎么写，决定了你的目标函数怎么构建。

"""

# %% [markdown]
"""
### 随堂试金石：焦耳定律中的身份识别 🕵️‍♂️

假设我们在实验室进行一个非常经典的测试：研究一段通电电阻器在通电一段时间后所产生的热量。
使用的核心物理公式是**焦耳定律**变体：
$$ Q = \frac{U^2}{R} \cdot t $$
（其中 $Q$ 是产生的总热量，$U$ 是施加在两端的电压，$R$ 是电阻器的阻值，$t$ 是通电时间。）

在这个特定的实验情境中：**你挑选了一个固定的电阻器，并将其连接到一个输出稳定电压的直流电源上**。然后你打开秒表，开始记录随着时间流逝，总共散发了多少热量。

请问，在这个实验对应的数学模型中，对这些物理量身份划分**最准确**的是哪一项？

- A. $R$ 是参数；$U$ 和 $t$ 是自变量；$Q$ 是因变量
- B. $Q$ 是参数；$R$ 和 $U$ 是自变量；$t$ 是因变量
- C. $t$ 是参数；$R$ 和 $U$ 是自变量；$Q$ 是因变量
- D. $R$ 和 $U$ 是参数；$t$ 是自变量；$Q$ 是因变量

"""

# %%
