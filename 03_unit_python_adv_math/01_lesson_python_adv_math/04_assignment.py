# %% [markdown]
"""
# 第三单元：进阶数学建模与计算 - 综合大作业

在本单元的最终作业里，你需要独立完成两个具有挑战性的模型。

## 作业 1：软绳单摆与抛体系统（微分方程复合建模）

**场景描述**：
一个小球（质量设为 $m=1 \text{kg}$）被一根长度为 $L=2 \text{m}$ 的柔软细绳吊着，悬挂在天花板上。
初始状态下，小球自然下垂静止（最低点）。突然在 $t=0$ 时，给小球施加一个向右的水平初速度 $v_0 = 8 \text{m/s}$。
小球在运动中会受到与速度成正比的空气阻力影响（阻力系数 $c=0.5 \text{N\cdot s/m}$）。重力加速度 $g=9.8 \text{m/s}^2$。

**挑战提示（必读）**：
因为绳子是**软的（不能产生向外的推力支承小球）**，所以小球在往上偏摆的过程中，有以下两种终结命运：
1. **命运A：正常秋千摆动达到最高点** -> 虽然一直有空气阻力损失动能，但没有冲过离心力临界线，速度先减为0。此时要求**终止仿真**。
2. **命运B：受力失衡导致绳子松弛（软绳跌落）** -> 运动到上半圆时，由于速度不够，产生的离心力加上重力的径向分力无法维持绳子的张力（即绳子张力 $T \le 0$）。此时绳子不再拉直，小球会像被抛出的石头一样，直接进入受空气阻力影响的**抛体掉落运动**。如果是这种情况，请在这发生的一瞬间**终止单摆计算**。

**【任务要求】**：
建立基于夹角 $\theta$（定义最低点 $\theta=0$，向右为正）的单摆微分方程，运用 `solve_ivp` 和**Event探测器（最高点、张力松弛极限）**追踪小球在这个阶段的运动，画出小球的 x-y 运动轨迹（直角坐标系）。

*物理公式帮助：*
- 沿切线的运动方程：$m L \theta'' = -m g \sin\theta - c L \theta'$
- 绳子内部张力公式：$T = m L (\theta')^2 + m g \cos\theta$

"""

# %% [markdown]
"""


"""

# %%
import numpy as np
import matplotlib.pyplot as plt
from scipy.integrate import solve_ivp

# 系统物理参数
m = 1.0
L = 2.0
g = 9.8
c = 0.5
v0 = 8.0

# 状态量解析：我们将状态 y向量定义为 [theta, omega]
# 初始角度 theta_0 = 0，初始角速度 omega_0 = v0 / L
y0 = [0.0, v0 / L]

# TODO 1: 编写微分方程
def pendulum_dynamics(t, y):
    theta, omega = y
    # 求解 theta 的二阶导数 (即 omega 的一阶导数)
    pass

# TODO 2: 编写监控事件 A - 捕捉小球走到最高点 (即角速度恰好减为 0 且发生了翻转)
def event_highest_point(t, y):
    pass

# TODO 3: 编写监控事件 B - 捕捉绳索失去张力 (即张力 T <= 0 的瞬间)
def event_slack_string(t, y):
    pass

# TODO 4: 设置这俩事件的属性 (全部设置为拦截终止 terminal=True)
# 并使用 solve_ivp 驱动计算

# TODO 5: 将求解得到的 theta(t) 极坐标转换回 x(t), y(t) 直角坐标并绘图！
# 极坐标转换关系参考：
# x = L * np.sin(theta)
# y = -L * np.cos(theta)

# %% [markdown]
"""
## 作业 2：蒙特卡洛通勤耗时评估

**场景描述**：
假设小明每天早上去上班要坐公交车。根据长期的习惯抽样，她早上出门有 **70%** 的概率坐 A 路车，有 **30%** 的概率坐 B 路车。

- **A路公交**：从起点到公司总共有 **6 个行驶段**（中途有 5 个停靠站）。
  - 每个行驶段在路上开的时间是 **2 分钟或 3 分钟**（各 50% 概率）。
  - 中途每个站停靠上下客时间是 **0.5 分钟或 1 分钟**（各 50% 概率）。

- **B路公交**：虽然绕路但更快。从起点到公司总共有 **8 个行驶段**（中途有 7 个停靠站）。
  - 每个普通行驶段的时间是 **1 分钟或 2 分钟**（各 50% 概率）。
  - 中途每站停靠的时间同样是 **0.5 分钟或 1 分钟**（各 50% 概率）。
  - **【隐藏机制】**：如果 B 路车出现**连续两个**停靠站都等了最久的 1 分钟（这让司机非常急躁），那么下一段紧接着的**行驶段**会一路踩油门，路程耗时将固定缩短为 **0.5 分钟**。

**【任务要求】**：
利用蒙特卡洛方法大量随机模拟小明的通勤过程（如模拟 10 万次）：
1. 算出小明平均需要多长总时间才能到公司？
2. 如果小明必须在每天 8点00分 上班打卡，为了保证 **90%的概率绝不迟到**，他在家最晚应该在早晨几点几分出发？

"""

# %% [markdown]
"""


"""

# %%
import numpy as np

simulations = 100000
total_times = []

# 开始蒙特卡洛模拟
for _ in range(simulations):
    # TODO 1: 决定今天搭乘哪一路车？(A路还是B路)
    # 用 np.random.rand() 试试身手
    
    # TODO 2: 如果是 A 路车，分别抽样 6 段路和 5 个站的耗时
    
    # TODO 3: 如果是 B 路车，除了基本抽样，注意处理“连停两次 1 分钟，则下一段狂飙 0.5 分钟”的关联判定
    
    # 将本次上班最终耗时记录入 total_times 列表中
    pass
    
# TODO 4: 平均耗时统计

# TODO 5: 计算以 90% 概率不迟到的极限通勤耗时（即求数据的 90 分位数）
# 提示：利用 np.percentile() 求取分位边界
# 最后由打卡的 8:00 倒推最晚出门时间！
