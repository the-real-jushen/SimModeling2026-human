# %% [markdown]
"""
欢迎来到第四单元综合实践！在这个挑战中，我们将结合本单元学到的所有知识点，解决一个带有物理网络阻抗特性（潮流）的综合调度问题。

# 单元综合实践：最优潮流模型同台竞技

在刚刚结束的两节课中，我们分别学习了：
1. **线性规划 (LP) / 分段线性化**: 速度极快，不会陷入局部最优，能够处理包含混合逻辑的大规模问题（对应 PuLP）。
2. **非线性规划 (NLP)**: 能够最精确刻画物理世界的真实曲线方程，但容易陷入局部坑洼（对应 SciPy Minimize）。

现在的这个大作业，正是电力系统等工业界里真实存在的**最优潮流分析 (Optimal Power Flow, OPF)** 的极简雏形。你需要运用所学，分别编写两套代码来解决同一个问题！

## 问题描述背景：加入网架约束
系统依然有两台发电机 $G_{1}$ 和 $G_{2}$，负荷为 100 MW。但这次，机组发出的电必须经过**有容量上限的高压输电线路**。
这就引入了**直流潮流约束 (DC Power Flow)**。网架结构的物理阻抗特性被提取成了 “转移分布系数矩阵（$S$ 阵）” ：
$$
S=\left[
\begin{matrix}
0 & -7/12 & -1/4 \\
0 & -5/12 & -3/4 \\
0 & 5/12 & -1/4
\end{matrix}
\right]
$$

### 数学模型汇总 (复习并添加新约束)

**1. 目标函数 (成本极小化)**
$$ F = (0.0007P_{G1}^2+0.30P_{G1}+4) + (0.0004P_{G2}^2+0.32P_{G2}+3) $$

**2. 等式功率平衡约束**
$$ P_{G1} + P_{G2} = 100 $$

**3. 发电机出力上下限**
$$ 30 \le P_{G1} \le 150 $$
$$ 0 \le P_{G2} \le 50 $$

**4. 线路潮流上下限 (本次新增重点)**
网络中共有 3 条核心线路，它们承载的有功潮流 $PL$ 计算规则是 $S$ 矩阵乘以当前的节点注入功率向量。
这三条线路的容量红线分别被限制死在 30、80、50 MW内。表示为矩阵不等式如下：
$$
-\left[
\begin{matrix}
30 \\ 80 \\ 50
\end{matrix}
\right]
\le 
S \cdot
\left[
\begin{matrix}
P_{G1} \\ P_{G2} \\ -100
\end{matrix}
\right]
\le
\left[
\begin{matrix}
30 \\ 80 \\ 50
\end{matrix}
\right]
$$

"""

# %% [markdown]
"""
### 动手实战一：使用 SciPy 攻克非线性网络约束

第一场竞技，请你担任算法工程师完成**非线性规划求解 (NLP)** 的底层代码撰写。
这是原汁原味的模型硬解，最关键的操作是**处理多维网络矩阵的上下限**。

**操作提示：**
* 约束函数返回值必须是一个一维数组（或列表）。
* 由于 3条线路 每条都自带“最大正向潮流”和“最大反向潮流”限制（上界和下界），你需要将 `PL <= Limit` 改写为 `Limit - PL >= 0`；将 `-Limit <= PL` 改写为 `PL + Limit >= 0`。最终共向 Scipy 返回 6 个约束值。

请在下方代码框中实现并输出最优调度计划：

"""

# %%
import numpy as np
from scipy.optimize import minimize

# ==========================================
# 任务 1：使用 SciPy 原生 非线性规划 求解 (NLP)
# ==========================================
def objective(x):
    # x[0] 为 PG1, x[1] 为 PG2。
    # TODO: 请返回原汁原味的二次连续成本方程
    pass

def eq_constraint(x):
    # TODO: 返回等式功率平衡约束 (别忘形式为: 公式片段 - 100 == 0)
    pass
    
def ineq_constraint(x):
    # TODO: 返回矩阵网络约束 (3条线路的安全边界)
    # SciPy 的 ineq 规定，所有的元素都必须 >= 0 审核才算通行！
    # 每条线路有一个上限、一个下限要求，共 6 个门槛！你可以返回包含6个元素的 numpy 数组。
    S = np.array([
        [0, -7/12, -1/4],
        [0, -5/12, -3/4],
        [0, 5/12, -1/4]
    ])
    
    # --- 请在下面计算潮流数组并包装组装那6枚 >=0 的通行条件 ---
    # 矩阵计算提示: PL_vector = S.dot([x[0], x[1], -100])
    pass

bounds = [(30, 150), (0, 50)]

# TODO: 请自行组装你的 cons 字典数组，并呼叫 minimize 推演 
# [别忘了加上 x0 的初值猜测哦！]
# res = minimize(...)

# %% [markdown]
"""
### 动手实战二：使用 PuLP 实现分段线性逼近

第二场竞技，让我们使用经典且稳健的**分段近似线性化算法 (Piecewise LP)**。
在测试过精确的非线性方法后，你可能会觉得 SciPy 非常方便。但正如前文所述，当问题扩大到几千台机组时，线性模型的绝对收敛性展现出了无可比拟的工业稳定性！

**要求：**
1. 请你回忆上一节的手法，将 $G_1$ 分为 `(30~90)` 和 `(90~150)` 两段；$G_2$ 分为 `(0~25)` 和 `(25~50)` 两段。
2. 自己手写完整的 PuLP 流程，并在添加约束时，把 S阵 展开得到的 3 条线路方程（带入负荷 $-100$计算常量）手动敲入 PuLP 的不等式中。

请完成代码并计算出力和近似总花费。

"""

# %%
import pulp

# ==========================================
# 任务 2：使用 PuLP 分段线性化 求解 (MIP/LP 近似法)
# ==========================================
# 策略：PG1 分成了两段 (30~90, 90~150)，PG2分成了两段(0~25, 25~50)。
# TODO: 完全独立手写你的 PuLP 模型。你可以翻看上一节的代码片段进行仿写！
# 别忘了用 '+=' 把这刚才手算出的那三根神秘的 S 阵线路潮流限制给敲上去！

prob = pulp.LpProblem("Opf_Assignment_Piecewise", pulp.LpMinimize)

# 提示： Line 1 的潮流公式解开为 PL1 = (-7/12) * PG2 + (-1/4)*(-100) = -7/12*PG2 + 25
# 所以约束应为： prob += (-7/12 * PG2 + 25 <= 30) 以及 prob += (-7/12 * PG2 + 25 >= -30)

# 你的代码...

# %% [markdown]
"""
## 结语：工业软件的底层选择与艺术

当你完成了这个大作业，你会惊奇地发现两种算法给出的机组出力解极其趋近！

在工业界复杂的软件底层设计体系（如各省级电网使用的调度平台软体）中，这两种路线各显神通：
*   面对 **日内分钟级/超短期的调度微调**（往往只涵盖几百个参数，要求给出极高精度的物理可行解）：我们舍得砸算力去运行 **SciPy / 高级内点法 (IPOPT) 等非线性引擎**。
*   面对 **包含数千条线路甚至上万节点的省级机组全周期日出清**框架：考虑到算力边界和对陷入死结的零容忍，系统几乎清一色都会做**多层级的割线、阶梯化代换**，采用商用的 **大模量 LP/MIP 求解器（如 Gurobi, CPLEX）**，保证庞大的网架计算百分之百能在数十秒内给出极其可靠、绝对收敛的全局排产结果！

"""
