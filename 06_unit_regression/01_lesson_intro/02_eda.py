# %% [markdown]
"""
欢迎来到探索性数据分析（EDA）环节！在你获取了一堆杂乱无章的数据，并且没有任何成熟建模思路时该怎么办？这节课我们将带你直接看透数据的本质。

# 探索性数据分析 (Exploratory Data Analysis, EDA)

在我们开始建立复杂的回归或分类模型之前，我们首先需要了解我们的数据。**探索性数据分析 (EDA)** 是一种初步的分析方法，它**不需要建立任何模型**，就可以让我们对数据建立起初步的认识，找到总体的规律。

通常 EDA 是通过**描述性统计**与**数据可视化 (画图)** 的方法直观地展现出来的。通过 EDA，我们可以大概知道数据有什么特性，这能帮助我们后面更有效地建立模型，有时候甚至能直接得到很多有用的规律，得到比模型本身更有用的信息。不需要庞大的算力，画几个图，算几个基础指标，数据的本质就展现在我们面前。

![](images/2020-03-24-18-01-07.png)

"""

# %% [markdown]
"""
## 1. 数据的本质与组成成分

数据是个很广的概念，在数据科学里面我们一般认为数据是信息的一种表示方法。
**数据一般是通过观测获取的，一套数值的、定性或者定量描述某一个事物属性的变量集。**
比如，“他长得好帅”，这个就不是一个很好的数据格式，但如果说，“他的颜值系数=7000”，或者“他的颜值属于【超帅】等级”，这些就是规范的数据了。如果你说“我猜小丽肯定喜欢他”，这就不是数据，因为它不是观测出来的客观值。

### 样本 (Sample)

说到数据有个十分重要的概念，就是**样本**。
数据是通过观测得到的，观测的过程也可以叫做采样 (Sampling)。**每一次采样过程，也就是每次观测得到的结果本身，就叫做一个独立的样本。**
采样是统计学里面的技巧，有很多种方法来保证获取的样本代表整体（population）的规律，我们在此不做展开。

我们的数据可以认为就是浩瀚样本的集合。如下面的表格，记录了病人的基本临床信息和未来十年内中风概率。
* **每一块横行 (Row)**：就是一个样本，代表了一个具体的单独被观测对象（比如某一个病人）。
* **每一条竖列 (Column)**：代表了在统一维度下描述某一共性特征的一个观测变量指标，我们把它称为**特征 (Feature)** 或者属性（比如年龄、性别等）。
* **终极标签 (Label)**：有时候除了特征，样本里面还会有标签，一般就是我们未来希望通过历史经验规律计算、预测的变量目标（比如下表格最右侧是否中风），这个预测标的被称为**标签**。

![](images/2020-03-21-12-32-21.png)

"""

# %% [markdown]
"""
### 数据科学的一般套路

![](images/2020-03-21-12-27-12.png)

数据科学的工作套路基本就像上图这般：
1. **收集数据 (Sampling)**。
2. **清洗和预处理**：因为数据里面可能有很多我们用不上的特征，或者是搞错了的样本，我们需要把这些数据整理干净。
3. **训练 (Train / Fit / Estimate)**：将彻底理顺的干净数据送到一种特定的算法里，让它内部自我磨合建立一套判断体系，这个建立经验特征大网的过程叫做**训练模型**。
4. **预测 (Predict / Inference / Forecast)**：当我们对打造出的模型通过各种测算认为满意了之后，把未来产生的**新收集到的未知结果的数据**提供给它，由它根据经验告诉我们答案结结论（例如对奇怪的东西进行分类，或者预测未来）。这个过程就是**业务预测推断**。

"""

# %% [markdown]
"""
## 2. 认识你的数据种类

如同前面所述，数据本身是个广义词，所以在做统计或画图之前，你必须敏锐地分清手中的特征数据到底属于什么根基类型，主要分为两大类：

### 2.1 类型数据 (Categorical data)
类型数据意味着它的取值是有限的，只包含几个特定的类型词汇库。例如性别，只有男和女，最多还有个不男不女；但绝没有“70%男”这种说法。它又分为两种：
* **Nominal (无序定类名目)**：种类之间没有绝对等级顺序或者大小之分。比如刚刚说的性别（男、女），是不存在先后的；再比如汽车品牌（大众、尼桑），它们不存在谁更靠近前面的说法。
* **Ordinal (有序排序递进)**：种类之间是有顺序的，存在明显的强弱进阶关系。比如成绩分为 A、B、C 三等，这里面 A 明确要成绩好于 B，而 B 又好于 C；再或者是衣服尺码（小、中、大），都是有梯度的。

### 2.2 连续数据 (Continuous data)
连续数据就是如同它的字面意思，它的取值有无限多种可能。一般也都分为两块：
* **Interval (等距量度数据)**：这个概念你可以理解为一个数值，它有绝对的单位，但**没有绝对的 0 点**。比如智商假设特朗普智商 60，爱因斯坦智商 120，你智商 180；那么爱因斯坦比特朗普聪明多少，你就比爱因斯坦聪明多少，这就是等距。但你不能说你比特朗普聪明 3 倍，因为智商没有绝对的 0 智商点（即使有个人考 0 分，不代表他没有任何智力）。温度也是同理。
* **Ratio (等比量度数据)**：和 Interval 没太大区别，但它带有绝对的真正 0 点意义。比如购买商品花费金钱、体重或空间长度。因为金钱 0 元意思是彻底没有，或者长度0意味着不存在该长度，所以这时候 20% 就是 10% 的 2 倍，20 万元的车实打实是 10 万元车的两倍价格。

"""

# %% [markdown]
"""
## 3. EDA 经典实战：1985 年汽车大全

接下来我们要动真格的了！我们导入一个极其经典的 1985 年汽车历史规格大全数据集（文件名为 `auto_clean.csv`）来进行第一把破冰大试探（EDA）。
你看看不需要那些天花乱坠的深层 AI 模型算法，仅凭最浅层的数据统计和画图，我们就能洞见何种隐藏的商业奥秘？

"""

# %%
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns

# 读取数据文件到变量 df（DataFrame 数据框机制格式）
df = pd.read_csv('../../data/06_unit_regression/01_lesson_intro/auto_clean.csv')

# 调用 df.head() 极为迅速地探查拉出打印头前五条样本的大面貌
df.head()




# %% [markdown]
"""
### 3.1 描述性统计

我们来首先检视到底我们有哪些好数据能用。
可以看到里面很多是属于数值型的特征：像长短度 `length`, 宽度 `width`, 卖价 `price`, 发动机暴走马力 `horsepower`，全属于典型的**连续数据 (Interval/Ratio)**。
还有一些纯文字描述的离散特征：像牌子商标 `make` 是 **Nominal 类型数据**；至于马力强弱等级 `horsepower-binned` 则是具有前后比较意义的 **Ordinal 类型数据**。

对于连续数据，既然想知道它大概是个什么分布特性，我们就得动用它本身的平均大线、中位数、极值范围、或者统计学常提及的离散程度（Variability）。
我们可以通过调用 `df.describe()` 这个指令，快速瞬间获取数据总体的基本描述分布数字。
我们来测试一下 `describe()` 方法，并着重看一下其中 `price` 这一列的统计结果：

"""

# %%
# describe() 方法它在默认姿态下只对纯粹数字型的 numerical 数据列进行宏观推算
df_describe = df.describe()  

# 我们特意把售卖指导价那列大指标 (price) 的特征单独提取看仔细些：
df_describe['price']




# %% [markdown]
"""
通过上面的指令输出，其实结果极为清晰：此表收集的历史市面上你能买到的汽车最便宜价格 (Min) 是 5118 元，最高价天花板 (Max) 则达到了 45400。同时标准差 (std) 也很大，说明价格跨度很松散不集中。

单纯看数字其实还不够直观共鸣，对这类型分布探索，这时候最好的做法就是祭出数据可视化的神图——**箱线图 Boxplot** 和 **直方图** 来看一眼了。

**怎么看箱线图？**
- **肚子中间的那条线**：中位数线，代表着位于正中间位（50%位置）的数值。
- **肚子本身的矩形方框 (Box)**：包含的是 Q1(25%刻度) 和 Q3(75%刻度) 之间的范围，也就是说它将市面上最主力的 50% 总体数据完全圈在了盒子里面的核心地带。
- **最外部两边拉出的“胡须”界限**：在箱子上下延伸的横线代表了统计学上所认为正常的正常数据上下限估算范围（基于 1.5 倍 IQR 容差）。
- **盒子外面的小圈圈 (最重要！)**：如果看到突破外界限飞出的点，那就是数据体系里的**异常值 (Outliers)**！这是非常有发现价值的家伙！

"""

# %%
# Visualization ———— Boxplot 箱线统计查看
plt.figure()
plt.boxplot(df['price'])
plt.title('Quick Boxplot of Whole Auto Prices Check')
plt.ylabel('The Ultimate Sell Price Range')
plt.show()

# 此外，也可以通过直方分布堆叠柱图 (Histogram) 来看看分布形态
plt.figure()
# bins 为你想看到多少柱形隔离堆垛区，edgecolor 提供清晰的黑线边界
plt.hist(df['price'], bins=30, edgecolor='black', color='skyblue')
plt.title('Histogram Frequency Look into Auto Prices')
plt.xlabel('Price Region Values')
plt.ylabel('Amount count of models')
plt.show()




# %% [markdown]
"""
**商业意义启示 (EDA 实战价值)：**
通过箱线图突破天际的几个异常值点，以及直方图形成的长长逐渐递减的右拖尾（右偏分布）你能看出什么？
答案是：在这汽车市场极度真实的反映了“平价车扎堆拼杀量大，而豪车价格极端离谱且款型极少”。这几个远远飞在正常圈外的圆圈和那瘦长的尾巴，揭露出了昂贵轿车的高溢价奢侈孤冷定位。你看，只通过这两条画图代码，不需要分析模型我们就解构了产品分布逻辑！

"""

# %% [markdown]
"""
### 3.2 离散类型数据的面貌绘图

咱们刚才讲述分析的都是连续数据。若是对待类型数据该怎么看呢？比如我们想看看当时市场上车子有什么样式（掀背车？还是两门敞篷？这种分类数据）。
对于这些不带有量值仅带有种类的 Categorical 数据，最恰当的方法就是使用 **柱状统计条图 (Bar Chart)** 或者直观占比的 **饼图 (Pie Chart)** 来看。

"""

# %%
# Visualization ———— Categorical
# 利用 value_counts() 统计每个形态种类的具体数量
body_style = df['body-style'].value_counts()

# 生成一个简易的各类体型的数量大柱子画图
plt.figure(figsize=(6,4))
plt.bar(x=body_style.index, height=body_style, color='orange')
plt.title('Comparing Bar Counts Distribution within Styles')
plt.xlabel('Varied Body Style Series Names')
plt.ylabel('Amount Count')
plt.show()

# 接下来绘制饼图查看占比 (autopct 可以在饼图切片上打印具体百分百)：
plt.figure(facecolor='white')
plt.pie(body_style, autopct='%1.1f%%', labels=body_style.index, shadow=True)
plt.title('Percentage Breakdown By Body Styles')
plt.show()




# %% [markdown]
"""
**实际意义解读：**
大家可以看到 `sedan`（最常见的老式平底轿车）和 `hatchback`（像高尔夫那类的掀背车，极为方便载货）共同霸占了超过了 80% 的总体量！它们就是广大劳动群众最需要的走量爆款！那些只能坐两个人拿来招摇的 `hardtop` 跟敞篷 `convertible` 占比可谓极少。通过极为简单的图表，各类车型的市场主旋律偏好即刻体现！这种绘图法对所有 Nominal 和 Ordinal 类数据都通吃有效。

"""

# %% [markdown]
"""
### 3.3 探索多重变量之间的关联（非常重要知识点）

数据分析最精华的地方就是挖掘多个特征它们彼此是否有暗中勾结关联。比如越费油的车一定卖得越贵吗？

**第一种情况：两者都是连续变量互相对比**

如果探究两个连续变化数字是否存在扯拉联系，首推绝招就是：**散乱的点阵关联绘图（Scatter plot 相关图散点）**。将它们挂到 X 与 Y 轴上一比较，谁也无法隐藏联系！
我们画画看“猛兽引擎力 (horsepower)”和“售价 (price)”有没有关联：

"""

# %%
# Visualization ———— Scatter
plt.figure()
plt.scatter(df['horsepower'], df['price'], alpha=0.6)
plt.title('The Critical Reveal Cross Analysis: Horsepower vs Sells Price')
plt.xlabel('Horsepower Force Rating')
plt.ylabel('Sells Price Line')
plt.show()




# %% [markdown]
"""
**实战意义看透：**这不仅非常直观还证实了我们的尝试，马力数字越往高冲，卖价简直是冲向云霄的垂直拉升！这种一方死咬着另一方攀升的关系也就是大名鼎鼎的 **正相关**。

当我们手握大量数字时，挨个手动画图太折磨了。我们要干票大的：把车价、高速油耗指数、车体宽度、吨位全拖出来，用命令 `scatter_matrix` 加上求它们彼此极道相关分数的 `corr()` 让电脑全自动对它们出具出相互打分体检表！

"""

# %%
# 将几个最具研究潜力的连续数值特征列拖出来进行组队大考察
corr_df = df[['price', 'highway-mpg', 'curb-weight', 'horsepower', 'length', 'width']]

# scatter_matrix 可以大开大阖将彼此所有可能的散点图全面阵列开火画出来
pd.plotting.scatter_matrix(corr_df, figsize=(8,8))
plt.show()

# 计算给出纯数计算推演出的相关系数矩阵 (Correlation Matrix)：
print("【各强特征数值底层的关联打分系数阵列表】")
print(corr_df.corr())




# %% [markdown]
"""
**这就是 EDA 大体检里最激动人心的关联分数结果时刻（越接向 1 越正相关，越靠向 -1 则意味着反向互斥倒退）：**
- 请仔细死盯寻找代表宽度的 `width` 以及重量底盘的数据 `curb-weight`。你看他们交集得分达到 0.86（极其正向相关逼近 1），说明用足钢铁把车身宽横向加粗加大，带来的必然是死沉重量！
- 但你看这整个表里负得分最大的一堆怨家！去找省油跑远能力（`highway-mpg`，数字越大跑得越远极省油）遇到吨位数据（`curb-weight`）交错时，出现**最深度的红字 -0.79 的超级不共戴天相关**！这意味着挖掘得出了汽车力学真知：只要打造沉重的庞然大车，那它必然是吞金耗油废柴（重车根本不可能节约燃料）。这完美契合物理守恒定理！

这证明我们收集的数据集极其真实并且健康！

"""

# %% [markdown]
"""
**第二种情况：类型变量 和 类型变量 交织怎么办？**

数字可以找牵扯，那要是例如想知道：生产商这些牌子 (`make` 列) 跟他们底盘车推力模式前驱还是后轮推 (`drive-wheels` 列) 有啥关联吗？！这就得端上有名的 **联立对列表格 (Contingency Table)** 以及最好借助强大的热图 **Heatmap** 把它渲染出颜色大图了！

"""

# %%
import seaborn as sns

# cross_tab 用来将两个毫无大小量级联系的类别文字列实现频率交叉制表
contingency_table = pd.crosstab(df['drive-wheels'], df['make'])

# 创建热图大底板并使用温差色彩 cmap 来展现这些聚集暗流：
plt.figure(figsize=(10,4))
sns.heatmap(contingency_table, annot=True, cmap='YlGnBu')
plt.title('Heatmap Color Tracking Plot: Brand vs Drive-wheels')
plt.show()




# %% [markdown]
"""
**分析商业底图真相揭秘：**这种染图技术谁颜色深，那里就扎堆！稍微扫一眼便立刻顿悟：大众 (volkswagen) 以及本田 (honda) 这些普通务实的人民轿车，清一色死压在成本更省的**前端起驱动 (`fwd`)区**格！但放眼观望代表极致驾驶体验但极贵空间的**后背起推（`rwd`区）**，全都被超跑高端世家宝马(bmw)、保时捷(porsche)完全瓜分。此外仅仅冷酷粗狂四轮联动（`4wd`）的领域，有独行侠斯巴鲁 (subaru)霸占。一瞬间不需要问厂商调研就洞察了各家产业定位！

"""

# %% [markdown]
"""
**第三种情况：类别类型 跟 连续变量互相交叉打架怎么办？**

这必须再度迎回我们的神形大杀器：基于各种纯种类箱子排列进行数值大冲高的 **多箱分布组合 (Boxplot进阶)**。我们将车分类做底部铺排，让刻度代表直接叫卖标价。一眼洞穿哪类到底价格区间能狂傲冲天。

"""

# %%
plt.figure()
# x给底座放各种文字形态，y让数值冲空高排去打架！
sns.boxplot(x=df['body-style'], y=df['price'])
plt.title('Hidden Price Limitation Extreme Cut Distribution Separated via Body Styles')
plt.show()




# %% [markdown]
"""
**财富大鸿沟读图破析：**
极其震撼！图面显示那群顶板死高的纯享受高级跑系敞篷车 (`convertible` 或 `hardtop`) 它们身子的下盘最低地板起跳线居然就已经极其夸张地比下面平民最广泛使用的便宜平顶掀背轿车（`hatchback`）它的绝对终极最高天花板还要高！彻底不在一个可讨论纬度。各种车型的地位被这几根小图线彻底判决了。

以上！这就是探索性分析数据的魅力版，不需要深奥运算靠敲几行画图和统计代码就有无数洞察浮出水面！接下来轮到你了：

"""

# %% [markdown]
"""
### 挑战一：长度与价格有什么羁绊吗？

基于咱们上面建立完毕的数据框架大表格对象 `df`，完成你的亲自破冰：
1. **绘制散点图**：提取字段 `length` 作为 X 轴横底，售价特征 `price` 作为 Y 直轴。看看长长的大车子真的会越来越贵吗？
2. **相关系数**：输出这俩参数之间最终极的相关拉扯系数打分（提示：可使用这行命令 `df['length'].corr(df['price'])`）。

"""

# %%
# ---------- 在黑框这里撰写第一个分析代码 ----------







# %% [markdown]
"""
### 挑战二：车辆空间尺度的四维大打架

探索**轴距 (wheel-base)** 与 **车辆长度 (length)**、**宽度 (width)**、**高度 (height)** 之间的相互关系！

1. 选出只包含这四个特征的表切片。
2. 试着打印出其中隐藏的关联得分系数阵列网。
3. （进阶任选）利用 `pd.plotting.scatter_matrix()` 把它们四者互相交叉连横的模样画出来！

"""

# %%
# ---------- 在黑框这里撰写第二个分析代码 ----------






